{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Film'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Film'|trans }}</h1>

    {{ include('film/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a href="{{ path('film_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('film/_delete_form.html.twig') }}
{% endblock %}
