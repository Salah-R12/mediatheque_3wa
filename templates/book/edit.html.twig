{% extends 'base.html.twig' %}

{% set mainTitle = 'Update "%s"'|trans|replace({'%s': book.name})  %}

{% block title %}{{ mainTitle }}{% endblock %}

{% block body %}
    <h1>{{ mainTitle }}</h1>

    {{ include('book/_form.html.twig', {'button_label': 'Update'|trans }) }}

    <a href="{{ path('book_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
