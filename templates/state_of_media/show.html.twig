{% extends 'base.html.twig' %}

{% block title %}StateOfMedia{% endblock %}

{% block body %}
    <h1>StateOfMedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state_of_media.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ state_of_media.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('state_of_media_index') }}">back to list</a>

    <a href="{{ path('state_of_media_edit', {'id': state_of_media.id}) }}">edit</a>

    {{ include('state_of_media/_delete_form.html.twig') }}
{% endblock %}
