{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Role'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Role'|trans }}</h1>

    {{ include('role/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a href="{{ path('role_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
