{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Music'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Music'|trans }}</h1>

    {{ include('music/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('music_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('music/_delete_form.html.twig') }}
{% endblock %}
