{% extends 'base.html.twig' %}

{% block title %}DigitalMedia{% endblock %}

{% block body %}
    <h1>DigitalMedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ digital_media.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ digital_media.url }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ digital_media.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('digital_media_index') }}">back to list</a>

    <a href="{{ path('digital_media_edit', {'id': digital_media.id}) }}">edit</a>

    {{ include('digital_media/_delete_form.html.twig') }}
{% endblock %}
