{% extends 'base.html.twig' %}

{% block title %}Borrow{% endblock %}

{% block body %}
    <h1>Borrow</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrow.id }}</td>
            </tr>
            <tr>
                <th>Borrow_date</th>
                <td>{{ borrow.borrowDate ? borrow.borrowDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Expiry_date</th>
                <td>{{ borrow.expiryDate ? borrow.expiryDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Return_date</th>
                <td>{{ borrow.returnDate ? borrow.returnDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('borrow_index') }}">back to list</a>

    <a href="{{ path('borrow_edit', {'id': borrow.id}) }}">edit</a>

    {{ include('borrow/_delete_form.html.twig') }}
{% endblock %}
