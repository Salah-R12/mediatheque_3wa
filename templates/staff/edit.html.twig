{% extends 'base.html.twig' %}

{% set mainTitle =  'Modifier %s'|trans|replace({'%s': staff.username}) %}

{% block title %}{{ mainTitle }}{% endblock %}

{% block body %}
    <h1>{{ mainTitle }}</h1>

    {{ include('staff/_form.html.twig', {'button_label': 'Update'|trans}) }}
	<br><br>
    <a href="{{ path('staff_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('staff/_delete_form.html.twig') }}
{% endblock %}
